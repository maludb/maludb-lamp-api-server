# Plan: Maludb-Dev-Setup.md

Create an educational, copy-paste-safe Ubuntu 24.04 setup guide from the user's draft.

## Todo
- [ ] Create `Maludb-Dev-Setup.md` at repo root
- [ ] 1a: add `resize2fs` after `lvextend` + `df -h` verification
- [ ] 1b: add `-y` to `apt upgrade`
- [ ] 2a: merge redundant php install lines; keep mysql + pgsql drivers
- [ ] New: enable mod_rewrite + AllowOverride for .htaccess (repo-specific)
- [ ] 3: clarify Composer PATH note; run `composer require` in /var/www
- [ ] 4: keep Node + Claude Code/Codex steps; add verification
- [ ] Test script: annotate placeholders, note URL + required pdo_pgsql
- [ ] Append to docs/activity.md
- [ ] Commit + push

## Review
(to be filled after work)
